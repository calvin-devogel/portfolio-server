// actually sike first: configuration
// plus: containerization
// next: telemetry
// then: authentication
// then: actual functionality (start with serving resume info)

#[tokio::main]
async fn main() -> anyhow::Result<()> {
    Ok(())
}